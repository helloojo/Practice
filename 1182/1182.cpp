#include <iostream>
using namespace std;
//BOJ #1182

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int n, s;
	cin >> n >> s;
	int arr[21];
	for (int i = 0; i < n; i++) {
		cin >> arr[i];
	}
}